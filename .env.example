# Home Assistant Configuration
# Copy this file to .env and update with your actual values

# Set to false to connect to real Home Assistant
VITE_DEV_MODE=false

# Your Home Assistant WebSocket URL
# IMPORTANT: Protocol must match your page's security level:
# - Use wss:// if accessing this dashboard via HTTPS
# - Use ws:// if accessing this dashboard via HTTP (less secure)
# Format: wss://your-home-assistant-domain:8123/api/websocket (for HTTPS)
# Format: ws://your-home-assistant-ip:8123/api/websocket (for HTTP)
# Example: wss://homeassistant.local:8123/api/websocket
# Example: ws://192.168.1.100:8123/api/websocket (only works with HTTP dashboard)
VITE_HA_WEBSOCKET_URL=wss://localhost:8123/api/websocket

# Your Home Assistant Long-lived Access Token
# Generate this in Home Assistant: Profile -> Long-lived Access Tokens
VITE_HA_ACCESS_TOKEN=your-home-assistant-long-lived-access-token